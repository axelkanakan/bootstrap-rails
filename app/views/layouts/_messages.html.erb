<% flash.each do |name, msg| %>
<% if msg.is_a?(String) %>
<div class="alert aert-<%= name == :notice ? "successs" : "error" %>">
<a class="close" data-dismis="alert">&#215;</a>
<%= content_tag :div, msg, :id => "flash_#{name}" %>
</div>
<% end %>
<% end %>